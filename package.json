{
  "name": "aws-shop-backend",
  "version": "1.0.0",
  "description": "Monorepo project for AWS shop backend",
  "author": "BorysovskaOA",
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "bootstrap": "npx cdk bootstrap --workspaces --if-present",
    "synth": "cdk synth --workspaces --if-present",
    "deploy": "cdk deploy --workspaces --if-present",
    "destroy": "cdk destroy --workspaces --if-present",
    "diff": "cdk diff --workspaces --if-present"
  },
  "workspaces": [
    "product-service"
  ]
}
