{
  "name": "aws-shop-backend",
  "version": "1.0.0",
  "description": "Monorepo project for AWS shop backend",
  "author": "BorysovskaOA",
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "bootstrap": "npm run bootstrap --workspaces --if-present",
    "synth": "npm run synth --workspaces --if-present",
    "deploy": "npm run deploy --workspaces --if-present",
    "destroy": "npm run destroy --workspaces --if-present",
    "diff": "npm run diff --workspaces --if-present"
  },
  "workspaces": [
    "import-service",
    "product-service"
  ]
}
